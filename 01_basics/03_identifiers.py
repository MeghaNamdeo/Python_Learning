''' 
1.idetifiers can be combination of uppercase and lowercase letters , digits or an
underscore(_)so myVariable, variable_1, variable_for_print all are the valid python identifiers.
2. An   identifier can not start with digit. So while variable1 is valid , 1variable is not valid.
3.We cannot use special symbols like !#@%$ etc in our identifier.
4. identifier can be of any length.

'''

name = " megha"
print(type(name)) # type of variable

age = 20
print(type(age)) 

price = 90.89
print(type(price))

